# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "lab_27"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-15208c23-e12e.cloud.databricks.com

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://dbc-15208c23-e12e.cloud.databricks.com
      # We always use /Users/labuser12807304_1763931805@vocareum.com for all resources to make sure we only have a single copy.
      root_path: /Users/labuser12807304_1763931805@vocareum.com/.bundle/${bundle.name}/${bundle.target}
    run_as:
      # This runs as labuser12807304_1763931805@vocareum.com in production. We could also use a service principal here,
      # see https://docs.databricks.com/dev-tools/bundles/permissions.html.
      user_name: labuser12807304_1763931805@vocareum.com
